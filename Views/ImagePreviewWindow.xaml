<ui:FluentWindow
    x:Class="TextureSwapper.Views.ImagePreviewWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="AI Texture Preview"
    Width="500"
    Height="550"
    Background="{DynamicResource ApplicationBackgroundBrush}"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="CanResize"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="AI TEXTURE PREVIEW" Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Image24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:TextBlock
                Grid.Row="0"
                Margin="0,0,0,15"
                FontSize="18"
                FontTypography="Subtitle"
                FontWeight="SemiBold"
                Text="Texture Preview (Scroll to Zoom, Drag to Pan)" />

        <Border
            Grid.Row="1"
            Background="{DynamicResource ControlFillColorTertiaryBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            ClipToBounds="True"
            CornerRadius="8">
            <ScrollViewer
                x:Name="PreviewScrollViewer"
                Cursor="SizeAll"
                HorizontalScrollBarVisibility="Auto"
                PreviewMouseLeftButtonDown="OnMouseLeftButtonDown"
                PreviewMouseLeftButtonUp="OnMouseLeftButtonUp"
                PreviewMouseMove="OnMouseMove"
                PreviewMouseWheel="OnPreviewMouseWheel"
                VerticalScrollBarVisibility="Auto">
                <Grid
                    x:Name="ImageGrid"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <Image
                        x:Name="PreviewImage"
                        Width="256"
                        Height="256"
                        RenderOptions.BitmapScalingMode="NearestNeighbor"
                        Stretch="Uniform">
                        <Image.LayoutTransform>
                            <ScaleTransform x:Name="ImageScale" ScaleX="1.0" ScaleY="1.0" />
                        </Image.LayoutTransform>
                    </Image>
                </Grid>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,10,0"
                    Appearance="Secondary"
                    Click="OnZoomInClick"
                    Icon="{ui:SymbolIcon ZoomIn24}"
                    ToolTip="Zoom In" />
                <ui:Button
                    Margin="0,0,10,0"
                    Appearance="Secondary"
                    Click="OnZoomOutClick"
                    Icon="{ui:SymbolIcon ZoomOut24}"
                    ToolTip="Zoom Out" />
                <ui:Button
                    Appearance="Secondary"
                    Click="OnResetZoomClick"
                    Content="Reset Zoom"
                    Icon="{ui:SymbolIcon ArrowCounterclockwise24}" />
            </StackPanel>

            <ui:Button
                Grid.Column="1"
                Appearance="Primary"
                Click="OnCloseClick"
                Content="Close" />
        </Grid>
    </Grid>
</Grid>
</ui:FluentWindow>
